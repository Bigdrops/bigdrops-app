export function ItemLibraryPlaceholder() {
  return (
    <div className="rounded-2xl border border-dashed border-border bg-card px-6 py-10 text-sm text-muted-foreground shadow-sm">
      Item Library foundation is wired. The management UI can now mount on top of the typed summary, suggestion, and history hooks in this module.
    </div>
  )
}
