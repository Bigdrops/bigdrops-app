In §3.6, steps 5 and 6 (entities and entity_permissions SELECT) are marked PASS in the 'Before Fix' column. Given that both of those tables' RLS policies check membership via workspace_members (which was recursion-broken at that point), how did they return PASS before the fix was applied? Were steps 5–6 actually tested against a clean pre-fix baseline in the same session as steps 1–4, or were they only run after some fixes were already in place?"