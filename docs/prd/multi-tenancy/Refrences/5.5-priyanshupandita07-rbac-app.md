# Priyanshupandita07/RBAC-APP

> **STATUS: REFERENCE ONLY — NOT STATUTORY AUTHORITY.**

## Source

- Repository: https://github.com/Priyanshupandita07/RBAC-APP
- Inspected: 2026-09-05 (web search, read-only)
- Type: Minimal RBAC reference implementation
- License: Not specified in search results
- Stack: Vite, Supabase

## Why This Reference Exists

This is a minimal org RBAC + 24h invite implementation on Vite + Supabase — the closest stack match to BIGDROPS. It demonstrates a simple RBAC model with time-limited invitations.

## Relevant Architecture / Patterns

- **Vite + Supabase stack** — same frontend framework as BIGDROPS.
- **Minimal RBAC** — simple role-based access control with organization scoping.
- **24h invite expiry** — time-limited invitation tokens.
- **Org-scoped permissions** — permissions are scoped to the organization.

## What BIGDROPS Should Borrow

- The **Vite + Supabase** stack validation — proves RBAC works on this stack.
- The **24h invite expiry** pattern — validates BIGDROPS §9's invitation token expiry.
- The **org-scoped permissions** — validates BIGDROPS §3's workspace-scoped abilities.

## What BIGDROPS Must NOT Adopt

- The minimal RBAC model — BIGDROPS §3 is more comprehensive (action-permission bundles, editable roles).
- Any patterns that assume a simplified permission model.
- Specific code patterns — use as structural reference only.

## Known Limitations

- Minimal implementation; may not cover all production edge cases.
- Single-level organization — does not address entity hierarchy.
- No entity lifecycle (§8A) coverage.
- License may restrict usage.

## Bottom Line

Closest stack match (Vite + Supabase). Validates RBAC on this stack. The 24h invite expiry and org-scoped permissions are directly applicable. Must be extended for BIGDROPS's comprehensive permission model and two-level tenancy.
